---
---
<section id="signup" class="section">
  <div class="container-custom max-w-4xl">
    <div class="p-8 md:p-12 rounded-3xl bg-gradient-to-br from-opal-glow via-white to-opal-blush/40 dark:from-opal-ocean dark:via-gray-900 dark:to-opal-violet/30 shadow-xl transition-all duration-300">
      <div class="text-center mb-8">
        <h2 class="mb-4 text-3xl md:text-4xl font-semibold text-gray-900 dark:text-white">Your Inbox Deserves a Bit of Breathing Room</h2>
        <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          No spam. No overwhelm. Just thoughtful tips, form walkthroughs, and cozy reminders that someone’s got your back. Opal sends only what’s useful and nothing that makes you sigh.
        </p>
      </div>

      <div x-data="newsletterForm()" class="max-w-xl mx-auto">
        <template x-if="!success">
          <form @submit.prevent="submit">
            <div class="flex flex-col sm:flex-row gap-4">
              <div class="grow">
                <label for="email" class="sr-only">Email address</label>
                <input 
                  type="email" 
                  id="email" 
                  name="email" 
                  x-ref="emailInput"
                  placeholder="enter your email to join the cozy list 🐾"
                  required
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-opal-ocean"
                />
                <p class="text-red-500 text-sm mt-1" x-text="error" x-show="error"></p>
              </div>
              <button 
                type="submit"
                class="px-6 py-3 rounded-lg font-medium bg-opal-ocean text-white hover:bg-opal-violet transition-all duration-300 whitespace-nowrap"
                :disabled="submitting"
                x-text="submitting ? 'Subscribing…' : 'Subscribe & Snuggle In 🐾'"
              ></button>
            </div>
          </form>
        </template>

        <template x-if="success">
          <div class="text-center p-6 bg-opal-glow/30 dark:bg-opal-ocean/20 rounded-lg">
            <svg class="w-12 h-12 text-opal-ocean mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">You're officially cozy now.</h3>
            <p class="text-gray-600 dark:text-gray-300">
              Welcome to the cozy corner. Updates will be soft, irregular, and worth opening.
            </p>
          </div>
        </template>
      </div>

      <div class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
        By subscribing, you agree to our 
        <a href="/privacy-policy" class="text-opal-violet hover:underline">Privacy Policy</a> and 
        <a href="/terms-conditions" class="text-opal-violet hover:underline">Terms of Service</a>.
      </div>
    </div>
  </div>
</section>

<script is:inline>
  function newsletterForm() {
    return {
      success: false,
      error: '',
      submitting: false,
      async submit() {
        this.submitting = true;
        this.error = '';
        const email = this.$refs.emailInput.value;

        const res = await fetch('/api/subscribe', {
          method: 'POST',
          body: new URLSearchParams({ email })
        });

        const data = await res.json();

        if (res.ok && data.success) {
          this.success = true;
        } else {
          this.error = data.error?.error_description || data.error || 'Something went wrong.';
        }

        this.submitting = false;
      }
    };
  }
</script>
